# ========== CONFIGURACIÓN AUTOMÁTICA HABILITADA ==========
# Este proyecto detecta automáticamente el entorno y configura las URLs.
# NO necesitas configurar estas variables a menos que quieras override manual.

# 🏠 Desarrollo local: localhost:5173 → localhost:8000 (automático)
# 🐳 Docker local: localhost:5173 → localhost:8000 (automático)  
# ☁️ Producción: cualquier-ip:5173 → misma-ip:8000 (automático)

# URL de la API del backend (OPCIONAL - solo para override manual)
# Si no se especifica, se construye automáticamente usando window.location
# VITE_API_URL=http://127.0.0.1:8000

# ID de cliente de Google OAuth (opcional)
VITE_GOOGLE_CLIENT_ID=
