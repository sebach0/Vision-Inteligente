# ========== CONFIGURACIÓN AUTOMÁTICA HABILITADA ==========
# Este proyecto detecta automáticamente el entorno y configura CORS/HOSTS.
# NO necesitas configurar estas variables a menos que quieras control específico.

# 🏠 Desarrollo: CORS permite todos los orígenes (automático)
# 🐳 Docker: CORS permite todos los orígenes (automático)
# ☁ Producción: CORS permite todos los orígenes (automático)

# Django Configuration
DJANGO_SECRET_KEY=change-me-in-production
DJANGO_DEBUG=1

# Superusuario inicial (OPCIONAL - usado por el seeder si se define)
# DJANGO_SUPERUSER_USERNAME=admin
# DJANGO_SUPERUSER_EMAIL=admin@condominio.local
# DJANGO_SUPERUSER_PASSWORD=admin123

# Hosts permitidos (OPCIONAL - por defecto permite todos)
# DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

# Base de datos PostgreSQL
POSTGRES_HOST=db
POSTGRES_DB=condominio
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# URLs del frontend para CORS (OPCIONAL - se configuran automáticamente)  
# FRONTEND_URL=http://127.0.0.1:5173
# FRONTEND_URL_ALT=http://localhost:5173

# CORS configuration (OPCIONAL - por defecto permite todos los orígenes)
# CORS_ALLOW_ALL_ORIGINS=True

# Google OAuth2 (opcional)
GOOGLE_OAUTH2_CLIENT_ID=
GOOGLE_OAUTH2_CLIENT_SECRET=

# Redis
REDIS_URL=redis://redis:6379/0

# Email settings (para desarrollo con MailHog)
EMAIL_HOST=mailhog
EMAIL_PORT=1025
EMAIL_USE_TLS=False
EMAIL_USE_SSL=False
DEFAULT_FROM_EMAIL=noreply@condominio.local

# Django Sites Framework
SITE_ID=1

# django-allauth
ACCOUNT_EMAIL_VERIFICATION=mandatory

echo "" >> backend\.env.example
echo "# ========== CREDENCIALES DE IA ==========" >> backend\.env.example
echo "# Azure Computer Vision (Reconocimiento de Placas)" >> backend\.env.example
echo "AZURE_VISION_SUBSCRIPTION_KEY=tu_azure_subscription_key_aqui" >> backend\.env.example
echo "AZURE_VISION_ENDPOINT=https://tu-recurso.cognitiveservices.azure.com/" >> backend\.env.example
echo "" >> backend\.env.example
echo "# Google Cloud Vision (Backup de Placas)" >> backend\.env.example
echo "GOOGLE_APPLICATION_CREDENTIALS=/app/credentials/google-credentials.json" >> backend\.env.example
echo "" >> backend\.env.example
echo "# AWS Rekognition (Reconocimiento Facial)" >> backend\.env.example
echo "AWS_ACCESS_KEY_ID=tu_aws_access_key_aqui" >> backend\.env.example
echo "AWS_SECRET_ACCESS_KEY=tu_aws_secret_key_aqui" >> backend\.env.example
echo "AWS_REGION=us-east-1" >> backend\.env.example
echo "AWS_REKOGNITION_COLLECTION_ID=condominio-rostros" >> backend\.env.example